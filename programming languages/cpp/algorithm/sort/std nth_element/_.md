---
tags:
  - programming-language
  - cpp
  - algorithm
---
[[programming languages/cpp/algorithm/sort/_|<=]]

Функция `std::nth_element` из заголовочного файла `<algorithm>` используется для **частичной сортировки диапазона** таким образом, что **n-й элемент** (по порядку) оказывается в своей **окончательной позиции**, как если бы весь диапазон был отсортирован.

> ✅ Это очень эффективный способ:
> - Найти медиану,
> - Найти k-й по величине элемент,
> - Выполнить частичную сортировку без полной `std::sort`.

### Синтаксис

```cpp
#include <algorithm>

template<class RandomAccessIterator>
void nth_element(RandomAccessIterator first, RandomAccessIterator nth,
                 RandomAccessIterator last);

template<class RandomAccessIterator, class Compare>
void nth_element(RandomAccessIterator first, RandomAccessIterator nth,
                 RandomAccessIterator last, Compare comp);
```

- `first`, `last` — итераторы, задающие диапазон `[first, last)`
- `nth` — итератор на позицию, где должен оказаться n-й элемент
- После вызова:
  - Элемент на позиции `nth` — **правильный** (как в отсортированном массиве)
  - Все элементы **до `nth`** ≤ этому элементу
  - Все элементы **после `nth`** ≥ этому элементу

### Важные моменты

| Особенность                           | Объяснение                                         |
| ------------------------------------- | -------------------------------------------------- |
| Работает за O(n) в среднем случае     | Очень эффективно                                   |
| Не сортирует весь массив              | Только частично                                    |
| Не сохраняет порядок равных элементов | Как и `std::sort`                                  |
| Требует рандомизированных итераторов  | Подходит для `std::vector`, `std::array`, массивов |
| Изменяет исходный контейнер           | Не создаёт копию                                   |

### Лучшая практика

| Совет                                                                    | Почему                                              |
| ------------------------------------------------------------------------ | --------------------------------------------------- |
| Используйте `std::nth_element`, если нужно найти медиану или k-й элемент | Самый эффективный способ                            |
| Используйте `std::sort`, если нужно отсортировать весь массив            | `nth_element` не гарантирует порядок                |
| Проверяйте границы: `nth` должен быть в `[first, last)`                  | Иначе — UB                                          |
| Комбинируйте с `std::distance` для получения индексов                    | Полезно при анализе                                 |
| Избегайте `std::nth_element` в циклах без оптимизации                    | Может быть медленным при многократном использовании |

- [[std nth_element - primitive|for primitive]]