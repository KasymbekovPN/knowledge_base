---
tags:
  - programming-language
  - cpp
  - algorithm
---
[[programming languages/cpp/algorithm/set & sort range/_|<=]]

Функция `std::set_intersection` из заголовочного файла `<algorithm>` используется для **нахождения пересечения двух отсортированных диапазонов** — то есть, элементов, которые **присутствуют в обоих диапазонах**.

> ✅ Это аналог операции "AND" в теории множеств.  
Подходит для:
- Поиска общих элементов,
- Сравнения списков,
- Работы с `std::vector`, `std::set`, массивами и другими контейнерами.

### Синтаксис

```cpp
#include <algorithm>

template<class InputIterator1, class InputIterator2, class OutputIterator>
OutputIterator set_intersection(InputIterator1 first1, InputIterator1 last1,
                                InputIterator2 first2, InputIterator2 last2,
                                OutputIterator result);

template<class InputIterator1, class InputIterator2, class OutputIterator, class Compare>
OutputIterator set_intersection(InputIterator1 first1, InputIterator1 last1,
                                InputIterator2 first2, InputIterator2 last2,
                                OutputIterator result, Compare comp);
```

- Оба исходных диапазона **должны быть отсортированы**
- Результат также будет отсортирован
- Возвращает итератор на конец результирующего диапазона

### Важные моменты

| Особенность                                                                                                   | Объяснение                  |
| ------------------------------------------------------------------------------------------------------------- | --------------------------- |
| Оба диапазона **должны быть отсортированы**                                                                   | Иначе результат неопределён |
| Работает за O(n + m)                                                                                          | Очень эффективно            |
| Учитывает дубликаты: если элемент встречается `k` раз в `a` и `m` раз в `b`, он будет включён `min(k, m)` раз | Как в мультимножествах      |
| Не изменяет исходные диапазоны                                                                                | Только читает               |
| Может принимать лямбды, функции, функторы                                                                     | Для гибкого сравнения       |

### Лучшая практика

| Совет                                                          | Почему                                       |
| -------------------------------------------------------------- | -------------------------------------------- |
| Убедитесь, что оба диапазона отсортированы                     | Иначе результат будет некорректным           |
| Используйте `std::set_intersection` вместо `std::find` в цикле | Гораздо быстрее                              |
| Проверяйте дубликаты: `{1,2,2}` ∩ `{2,2,3}` = `{2,2}`          | `set_intersection` учитывает количество      |
| Комбинируйте с `std::sort`, если данные не отсортированы       | Для однократной проверки — может быть дороже |
| Избегайте `std::set_intersection` на неотсортированных данных  | Это нарушит логику                           |

- [[std set_intersection - primitive|primitive]]