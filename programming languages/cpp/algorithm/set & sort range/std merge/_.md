---
tags:
  - programming-language
  - cpp
  - algorithm
---
[[programming languages/cpp/algorithm/set & sort range/_|<=]]

Функция `std::merge` из заголовочного файла `<algorithm>` используется для **слияния двух отсортированных диапазонов в один отсортированный диапазон**.

> ✅ Это ключевая функция в алгоритме **сортировки слиянием (merge sort)** и идеально подходит для:
> - Объединения двух отсортированных контейнеров,
> - Работы с `std::vector`, `std::list`, массивами,
> - Эффективного слияния данных без полной сортировки.

---

## 🔍 Синтаксис

```cpp
#include <algorithm>

template<class InputIterator1, class InputIterator2, class OutputIterator>
OutputIterator merge(InputIterator1 first1, InputIterator1 last1,
                     InputIterator2 first2, InputIterator2 last2,
                     OutputIterator result);

template<class InputIterator1, class InputIterator2, class OutputIterator, class Compare>
OutputIterator merge(InputIterator1 first1, InputIterator1 last1,
                     InputIterator2 first2, InputIterator2 last2,
                     OutputIterator result, Compare comp);
```

- Возвращает итератор на конец результирующего диапазона
- Оба исходных диапазона **должны быть отсортированы**
- Результат также будет отсортирован

### Важные моменты

| Особенность                                         | Объяснение                            |
| --------------------------------------------------- | ------------------------------------- |
| Требует, чтобы оба диапазона были **отсортированы** | Иначе результат не будет отсортирован |
| Работает за O(n + m)                                | Очень эффективно                      |
| Не изменяет исходные диапазоны                      | Только читает                         |
| Может принимать лямбды, функции, функторы           | Для гибкого сравнения                 |
| Целевой контейнер должен быть достаточно большим    | Или используйте `std::back_inserter`  |

### Лучшая практика

| Совет                                                    | Почему                             |
| -------------------------------------------------------- | ---------------------------------- |
| Убедитесь, что оба диапазона отсортированы               | Иначе результат будет некорректным |
| Используйте `std::back_inserter`, если размер неизвестен | Чтобы избежать переполнения        |
| Используйте `std::greater` для слияния по убыванию       | Или свою лямбду                    |
| Комбинируйте с `std::distance` для получения размера     | Полезно при анализе                |
| Избегайте `std::merge` на неотсортированных данных       | Это нарушит логику                 |

- [[std merge - primitive|primitive]]
