---
tags:
  - programming-language
  - cpp
  - threads
---
[[_cpp|<=]]

## Сводная таблица

|Этап|Модуль|Линковка|Сложность|Время (ориентир)|
|---|---|---|---|---|
|1|Optional|нет|низкая|0.5 дня|
|1|Variant|нет|низкая|0.5 дня|
|1|Any|нет|низкая|0.5 дня|
|1|SmartPtr|нет|низкая|1 день|
|2|Container|нет|средняя|1 день|
|2|Filesystem|да|низкая|1 день|
|3|Regex|да|средняя|1 день|
|3|Spirit|нет|высокая|3–4 дня|
|4|Multiprecision|нет|низкая|0.5 дня|
|5|Graph (BGL)|нет|высокая|3–4 дня|
|6|Asio|частично|высокая|4–5 дней|
|6|Beast|нет*|высокая|3–4 дня|
|7|Test|да|средняя|1–2 дня|
|8|Python|да|средняя|2–3 дня|

* Beast зависит от Asio и его системных библиотек.

## Базовая настройка проекта (делается один раз)

### vcpkg.json

```json
{
  "name": "boost-learning",
  "version": "0.1.0",
  "dependencies": [
    "boost-asio",
    "boost-smart-ptr",
    "boost-filesystem",
    "boost-regex",
    "boost-spirit",
    "boost-multiprecision",
    "boost-beast",
    "boost-graph",
    "boost-test",
    "boost-python",
    "boost-container",
    "boost-optional",
    "boost-variant",
    "boost-any"
  ]
}
```

### CMakeLists.txt

```cmake
cmake_minimum_required(VERSION 3.21)
project(boost_learning LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Подключение конкретных компонентов добавляется по мере изучения, например:
find_package(Boost REQUIRED COMPONENTS filesystem regex)

add_executable(app main.cpp)
target_link_libraries(app PRIVATE Boost::filesystem Boost::regex)
```

Конфигурация с toolchain-файлом vcpkg:

```bash
cmake -B build -S . -DCMAKE_TOOLCHAIN_FILE=[путь]/vcpkg/scripts/buildsystems/vcpkg.cmake
cmake --build build
```

Совет: положи `CMAKE_TOOLCHAIN_FILE` в `CMakePresets.json`, чтобы не писать длинную команду каждый раз.

### CMakePresets.json
```json
{  
  "version": 3,  
  "configurePresets": [  
    {  
      "name": "default",  
      "generator": "Ninja",  
      "binaryDir": "${sourceDir}/.build",  
      "toolchainFile": "$env{VCPKG_ROOT}/scripts/buildsystems/vcpkg.cmake"  
    }  
  ]  
}
```

- [[000 boost optional|boost optional]]
- [[001 boost variant|boost variant]]
- [[002 boost any|boost any]]
- [[003 boost start ptr|boost start ptr]]
- [[004 boost container|boost container]]
- [[005 boost filesystem|boost filesystem]]
- [[006 boost regex|boost regex]]
- [[007 EBNF|EBNF]]
- [[008 boost spirit|boost spirit]]
- [[009 boost multiprecition|boost multiprecition]]