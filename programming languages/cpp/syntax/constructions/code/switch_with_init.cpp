#include <iostream>

using std::cout;
using std::endl;

int main(int argc, char const *argv[]) {
    char op {'+'};
    int n {2};

    switch (int k{3}; op)
    {
        case '+':
            cout << n << " + "  << k << " = " << n + k << endl;
            break;
        
        default:
            cout << n << " - "  << k << " = " << n + k << endl;
            break;
    }

    return 0;
}
