---
tags:
  - programming-language
  - cpp
  - syntax
  - operation
  - arithmetic-operation
  - floating-point
  - float
  - double
---
[[__cpp syntax arithmetic operations__|<=]]

При сложении или вычитании чисел с плавающей точкой, которые сильно отличаются по значению, следует проявлять осторожность. Например, сложим число __1.23E-4__ и __3.65E+6__. Мы ожидаем, что сумма будет равна __3650000,000123__. Но при преобразовании в число с плавающей запятой с точностью до семи цифр это становится следующим:
```
3.650000E+06 + 1.230000E-04 = 3.650000E+06
```

```cpp
#include <iostream>

using std::cout;
using std::endl;

int main(int argc, char const *argv[]) {
    float a {1.23e-4};
    float b {3.65e+6};
    float ab {a + b};

    cout << a << " + " << b << " = " << ab << endl;

    return 0;
}
```

```
0.000123 + 3.65e+06 = 3.65e+06
```

То есть первое число никак не изменилось, поскольку для хранения точности отводится только 7 цифр.

Также стоит отметить, что стандарт __IEEE__, который реализуется компиляторами __С++__, определяет специальные значения для чисел с плавающей точкой, в которых мантисса на бинарном уровне состоит только из нулей, а экспонента, которая состоит из одних единиц, в зависимости от знака представляет значения __+infinity__ (плюс бесконечность +∞) и __-infinity__ (минус бесконечность -∞). И при делении положительного числа на ноль, результатом будет __+infinity__, а при делении отрицательного числа на ноль - __-infinity__.

Другое специальное значение с плавающей точкой, определенное этим стандартом, представляет значение __NaN__ (не число). Это значение представляет результат операции, который не определяется математически, например, когда ноль делится на ноль или бесконечность на бесконечность. Результатом любой операции, в которой один или оба операнда являются `NaN`, также является `NaN`.
```cpp
#include <iostream>

using std::cout;
using std::endl;

int main(int argc, char const *argv[]) {
    double a {1.5}, b{}, c{}, d{-1.5};
    double ab {a / b};
    double dc {d / c};
    double bc {b / c};

    cout << "a / b => " << ab << endl;
    cout << "d / c => " << dc << endl;
    cout << "b / c => " << bc << endl;
    cout << "b / c + a => " << bc + a << endl;

    return 0;
}
```

```
a / b => inf
d / c => -inf
b / c => -nan(ind)
b / c + a => -nan(ind)
```

Для демонстрации рассмотрим следующую программу:

---
[Арифметические операции|metanit.com](https://metanit.com/cpp/tutorial/2.6.php)