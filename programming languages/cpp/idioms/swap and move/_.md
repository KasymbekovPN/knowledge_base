---
tags:
  - programming-language
  - cpp
  - RAII
---
[[programming languages/cpp/idioms/_|<=]]

Идиома **"Swap + Move"** — это современный, безопасный и эффективный способ реализации **оператора присваивания (`operator=`)** в C++.

> ✅ Она использует **RAII**, **move-семантику** и **обмен (swap)** для обеспечения:
> - Исключений-безопасности,
> - Автоматического управления ресурсами,
> - Отсутствия дублирования кода.

```cpp
class MyClass {
    int* data;
public:
    MyClass& operator=(const MyClass& other) {
        if (this != &other) {
            delete[] data;
            data = new int[other.size];
            std::copy(other.data, other.data + other.size, data);
        }
        return *this;
    }
};
```

❌ Проблема: если `new` выбросит исключение после `delete[]`, объект окажется в невалидном состоянии.

```cpp
#include <iostream>
#include <algorithm>
#include <memory>

class SArray {

private:
    std::unique_ptr<int[]> data;
    size_t size;

public:
    explicit SArray(size_t _size):
        data{std::make_unique<int[]>(_size)},
        size{_size} {}

    SArray(const SArray& _other):
        data{std::make_unique<int[]>(_other.size)},
        size{_other.size} {
        std::copy(
            _other.data.get(),
            _other.data.get() + _other.size,
            data.get()
        );
    }

    SArray(SArray&&) noexcept = default;

    SArray& operator=(SArray _other) noexcept {
        swap(_other);
        return *this;
    }

    void swap(SArray& _other) noexcept {
        std::swap(data, _other.data);
        std::swap(size, _other.size);
    }

    size_t getSize() const {
        return size;
    }
};

int main() {
    auto&& s0 = SArray{7};
    auto&& s1 = SArray{5};
    s1 = s0;
    std::cout
        << "s1 size: "
        << s1.getSize() << std::endl;

    return 0;
}
```

```
s1 size: 7
```

## ✅ Как это работает?

1. **Параметр передаётся по значению**:
   - При копировании: создаётся полная копия правого операнда,
   - При перемещении: используется move-конструктор.
1. **Выполняется `swap`** между `*this` и временным объектом `_other`.
2. **Старые данные остаются в `_other`**.
3. При выходе из функции `_other` уничтожается → вызывается его деструктор → старые данные освобождаются.

✅ Таким образом:
- Новые данные уже получены безопасно,
- Старые данные будут удалены автоматически,
- Исключения не нарушают инварианты класса.
